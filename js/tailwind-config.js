// js/tailwind-config.js

tailwind.config = {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Ubuntu', 'sans-serif'],
                cursive: ['Smooch Sans', 'cursive'],
                display: ['Orelega One', 'cursive'],
                decorative: ['Zeyada', 'cursive'],
                fancy: ['Monoton', 'cursive'],
            }
        }
    }
}